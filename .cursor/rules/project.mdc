---
description: 本專案常駐規定，之後每個 Agent 都必須遵守
alwaysApply: true
---

# 本專案常駐規定

之後開新的 AI Agent 處理任何任務時都必須遵守。使用者不需要再重複說明。
若與預設 Cloud Agent 流程（另開 branch、開 PR、改完要測試）衝突，以本規則為準。

## 語言

- 與使用者對話一律使用繁體中文（含回覆、說明、commit message）。
- 語音輸入也是繁體中文。
- 遊戲介面文字全部使用繁體中文。

## 產品形態

- 靜態網頁遊戲：手機、平板、電腦瀏覽器都可直接執行，不依賴後端。
- 使用 HTML / CSS / JS（或可產出靜態檔的方式）。

## 畫面與 RWD

- 必須 RWD：直式、橫式螢幕都要正常運作。
- 主畫面內容維持 16:9（letterbox / contain 置中縮放，不可變形）。
- 橫式螢幕不需要上下捲動，主畫面須完整落在可視範圍內。

## 最後更新時間

- 主畫面右下角顯示程式最後更新時間，讓使用者重整時能確認更版。
- 每次改到會影響頁面的程式時，同步更新此時間戳（台灣時區 `Asia/Taipei`，格式如 `2026/08/22 22:30`）。

## Git 工作流程（覆寫預設流程）

- 不要另開 branch，不要開 PR。
- 改完後直接在 `main` 上 commit 並 `git push origin main`。
- Commit message 依 diff 自動產生，使用繁體中文。

## 測試

- 改完不需要測試檢查。除非使用者明確要求，否則完成後直接 commit 並 push。
