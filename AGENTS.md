# 專案規定（之後開新 Agent 必須遵守）

這份文件是本專案的常駐規定。之後開新的 AI Agent 處理任何任務時，都必須完整遵守，使用者不需要再重複說明。

若與預設的 Cloud Agent 流程（另開 branch、開 PR、改完要測試）衝突，**以本文件為準**。

## 語言

- 與使用者對話一律使用**繁體中文**（包含回覆、說明、commit message）。
- 使用者語音輸入也是繁體中文，請以繁體中文理解與回覆。
- 遊戲介面文字全部使用繁體中文。

## 產品形態

- 這是**靜態網頁遊戲**：在手機、平板、電腦瀏覽器都可直接執行，不依賴後端伺服器。
- 使用 HTML / CSS / JavaScript（或可編譯成靜態檔的方式），可直接開啟或部署到靜態空間即可遊玩。

## 畫面與 RWD

- 必須具備 RWD：直式、橫式螢幕都要能正常運作。
- **主畫面內容維持 16:9**（以 letterbox / contain 方式置中縮放，不可變形）。
- **橫式螢幕不需要上下捲動**：整個主畫面要完整落在可視範圍內。
- 觸控與滑鼠操作都要可用。

## 最後更新時間

- 主畫面**右下角**必須顯示程式最後更新時間。
- 目的：使用者重整網頁時，能確認網頁確實更版。
- Agent 每次改到會影響頁面的程式時，都要同步更新這個時間戳（使用台灣時區 `Asia/Taipei`，格式清楚可讀，例如 `2026/08/22 22:30`）。

## Git 工作流程（覆寫預設 Cloud Agent 流程）

- **不要另開 branch，不要開 PR。**
- 每次改完檔案後，直接在 `main` 上 `git add`、`git commit`、`git push origin main`。
- Commit message 根據這次 diff **自動產生**，使用繁體中文、簡短說明改了什麼。
- 不要把變更留在工作區不提交。

## 測試

- 修改完成後**不需要測試檢查**（不跑測試、不開瀏覽器驗證、不做額外 QA），以節省時間。
- 除非使用者明確要求測試，否則完成修改後直接 commit 並 push。
